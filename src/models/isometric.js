const table = {
    TABLE: "list_isometric",
    COLUMN: {
        ID: "ID",
        PROJECT_ID: "PROJECT_ID",
        INPUT_BY: "INPUT_BY",
        INPUT_DATE: "INPUT_DATE",
        LINE_NO: "LINE_NO",
        ISO_NO: "ISO_NO",
    }
}

const QUERY = {
    get: ``
}

module.exports = {
    
}